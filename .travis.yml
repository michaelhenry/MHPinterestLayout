osx_image: xcode9.3
language: objective-c
jobs:
  include:
    # - stage: Testing
    #   before_install:
    #     - gem install cocoapods
    #   script: 
    #     - cd Example
    #     - pod install
    #     - xcodebuild test -workspace MHPinterestLayout.xcworkspace -scheme MHPinterestLayout-Example -sdk iphonesimulator -destination 'platform=iOS Simulator,name=iPhone X' -quiet
    # - stage: Swift Package Manager
    #   script:
    #     - swift build
    - stage: deploying to cocoapods
      before_install:
        - echo $TRAVIS_TAG
        - export MHPINTERESTLAYOUT_VERSION=$TRAVIS_TAG
        - gem install cocoapods
      script:
        - pod lib lint --allow-warnings --swift-version=4.2
        - pod trunk push --allow-warnings --swift-version=4.2
      if: tag IS present